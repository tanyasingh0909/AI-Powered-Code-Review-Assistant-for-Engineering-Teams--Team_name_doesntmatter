# ─── App Settings ────────────────────────────────────────────
APP_ENV=development
LOG_LEVEL=INFO

# ─── Encryption ──────────────────────────────────────────────
# Leave empty to auto-generate on first startup.
# If you set this, it MUST stay the same across restarts or stored credentials become unreadable.
ENCRYPTION_KEY=

# ─── CORS ────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:3000

# ─── API Authentication (optional — leave empty to disable) ──
API_KEY=

# ─── LLM Provider Defaults (fallback if no config set via UI) ─
# Active provider: anthropic | openai | gemini | deepseek | kimi | openrouter
LLM_PROVIDER=openrouter
LLM_MODEL=meta-llama/llama-3.3-70b-instruct:free
LLM_MAX_TOKENS=4096

# Provider API keys (only the active provider's key is needed)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=
XAI_API_KEY=
QWEN_API_KEY=
META_API_KEY=
KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn/v1
OPENROUTER_API_KEY=

# ─── Rate Limiting ───────────────────────────────────────────
RATE_LIMIT=10/minute

# ─── Safety ──────────────────────────────────────────────────
EXPLAIN_TIMEOUT_MS=30000
